{% extends 'base.html.twig' %}

{% block title %}Cartao index{% endblock %}

{% block body %}
    <h1>Cartao index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero</th>
                <th>Cvv</th>
                <th>DataValidade</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cartao in cartaos %}
            <tr>
                <td>{{ cartao.id }}</td>
                <td>{{ cartao.numero }}</td>
                <td>{{ cartao.cvv }}</td>
                <td>{{ cartao.dataValidade }}</td>
                <td>
                    <a href="{{ path('cartao_show', {'id': cartao.id}) }}">show</a>
                    <a href="{{ path('cartao_edit', {'id': cartao.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cartao_new') }}">Create new</a>
{% endblock %}
