{% extends 'base.html.twig' %}

{% block title %}Endereco{% endblock %}

{% block body %}
    <h1>Endereco</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ endereco.id }}</td>
            </tr>
            <tr>
                <th>Cep</th>
                <td>{{ endereco.cep }}</td>
            </tr>
            <tr>
                <th>Logradouro</th>
                <td>{{ endereco.logradouro }}</td>
            </tr>
            <tr>
                <th>Cidade</th>
                <td>{{ endereco.cidade }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ endereco.estado }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ endereco.numero }}</td>
            </tr>
            <tr>
                <th>Bairro</th>
                <td>{{ endereco.bairro }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('endereco_index') }}">back to list</a>

    <a href="{{ path('endereco_edit', {'id': endereco.id}) }}">edit</a>

    {{ include('endereco/_delete_form.html.twig') }}
{% endblock %}
